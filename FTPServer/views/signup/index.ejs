<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css"
      integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T"
      crossorigin="anonymous"
    />
    <title>Signup</title>
  </head>
  <body>
    <div class="navbar navbar-light justify-content-center">
      <h1>Fast Net FTP Server</h1>
    </div>
    <div class="row justify-content-center">
      <div class="col col-sm-7 col-lg-5 col-xl-3 mt-5 mb-5" id="login-box">
        <h2>Signup</h2>
        <br />
        <p class="text-danger"><%= alert %></p>
        <p class="text-danger">
          <% if (error){%>
            <%=error[0].msg%>
          <%}%>
        </p>
        <form method="post" action="/signup">
          <input
            class="form-control"
            type="text"
            placeholder="Adminstrative Password to Signup"
            name="aps"
            id="aps"
            onkeyup="validateAps()"
            autocomplete="off"
          /><br />
          <label for="sel1">Select User:</label>
          <select class="form-control" name="type" id="sel1">
           <option>admin</option>
           <option>moderator</option>
          </select>
          <br />
          <input
            class="form-control"
            type="text"
            placeholder="Full Name"
            name="name"
            autocomplete="off"
          /><br />
          <input
            class="form-control"
            type="text"
            placeholder="Username"
            autocomplete="off"
            name="username"
          /><br />
          <input
            class="form-control"
            type="email"
            placeholder="Email"
            autocomplete="off"
            name="email"
          /><br />
          <input
            class="form-control"
            type="password"
            placeholder="Password"
            name="password"
            autocomplete="new-password"
            id="password"
          /><br />
          <input
            class="form-control"
            type="password"
            placeholder="Confirm Password"
            name="confirmPassword"
            autocomplete="off"
            id="confirmPassword"
          /><br />
          <button class="btn-lg btn-dark btn-block" type="submit">
            Create Account</button
          ><br />
        </form>
        <div class="text-center">
          <a href="/login">Already have an account? Log in!</a>
        </div>
      </div>
    </div>
  </body>
  <!-- Password match check -->
  <script>
    var password = document.getElementById("password"),
      confirm_password = document.getElementById("confirmPassword");
     
     function validateAps(){
      var aps=document.getElementById("aps");

      if(aps.value=='1234'){
        aps.setCustomValidity("");
      }else{
        aps.setCustomValidity("Passwords Don't Match");
      }
       
     } 
    function validatePassword() {
      if (password.value != confirmPassword.value) {
        confirmPassword.setCustomValidity("Passwords Don't Match");
      } else {
        confirmPassword.setCustomValidity("");
      }
    }
    //aps.onchange = validateAps;
    password.onchange = validatePassword;
    confirmPassword.onkeyup = validatePassword;
  </script>
</html>

